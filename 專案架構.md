# TXF Data Lake 專案架構與核心解析

## 1. 專案用途
本專案為一個針對 **台指期 (TXF)**、**小台指期 (MXF)** 與 **加權指數 (TSE)** 打造的**資料湖 (Data Lake) 與 ETL 視覺化平台**。
主要用途包含：
- 透過永豐金 Shioaji API 獲取高頻 Tick 歷史資料並本地化儲存。
- 使用 **Polars** 高效進行資料清洗、重取樣 (Resample)，轉換為多種週期的 K 棒資料 (1s, 5s, 1m, 1h, 1d 等) 並以高效能的 Parquet 格式保存。
- 內建強大的互動看盤工具，基於 `lightweight-charts` 重建與 TradingView 相似的互動圖表，支援日夜盤合併、自訂指標與價格換月平滑校正等進階功能。

## 2. 目錄結構
專案模組化設計清晰，結構如下：

```text
txf-data-lake/
├── .gitignore              # Git 忽略清單
├── README.md               # 專案說明文件
├── requirements.txt        # 專案依賴套件表
├── AutoRun.md              # 自動化與批次執行操作手冊
├── batch_run.py            # TXF/TSE 批次下載歷史資料進入點
├── main_etl.py             # 每日增量下載與轉檔進入點 (ETL 主程式)
├── mxf_batch_run.py        # MXF 批次下載歷史資料進入點
├── fix_kbars.py            # 特殊狀況重製 K棒工具
├── view_chart.py           # 視覺化互動看盤主程式
├── adapters/               # 外部 API 適配層
│   └── shioaji_source.py   # Shioaji API 連線實作
├── config/                 # 全域設定檔與常數設定
│   ├── settings.py         
│   └── calendar_rules.py   
├── core/                   # 核心資料處理邏輯引擎
│   ├── loader.py           # 負責讀取 Parquet
│   ├── processor.py        # K棒衍生計算 (如均線、KD)
│   └── resampler.py        # Tick 轉換 K棒的核心演算法
├── visualization/          # UI 與視圖層
│   ├── chart_builder.py    # 建構 Lightweight Charts
│   └── style_config.py     # 配置圖表顏色與樣式
└── notes/                  # 策略與開發設計文件
```

## 3. 核心模組
此專案採用類似 ETL (Extract, Transform, Load) 的分層架構設計：

- **外部連線層 (`adapters`)**：
  - `shioaji_source.py` 負責處理登入、額度檢查與合約日期的 Tick 下載。
- **資料處理與計算層 (`core`)**：
  - `resampler.py` 負責高速將大量 Tick 聚合為 K 棒（支援分批與自動對齊）。
  - `processor.py` 提供將 K 棒算上各種技術指標、並標記日夜盤或合併盤次的後處理邏輯。
  - `loader.py` 管理讀取本地 Parquet 的快取與過濾功能。
- **ETL 調度層**：
  - `main_etl.py` 針對單日進行 `下載 -> 重取樣 -> 存檔`，具備本機資料庫快取判斷，已下載過即跳過。
  - `batch_run.py` 以迴圈方式包裹主程式以達成 `TXF/TSE` 批次補齊多日資料任務。
  - `mxf_batch_run.py` 提供 `MXF` 專用批次入口，方便獨立補歷史回測資料。
- **視覺化呈現層 (`visualization`)**：
  - `view_chart.py` 搭配 `chart_builder.py` 呼叫開源的前端渲染引擎 `lightweight-charts-python` 將 DataFrame 以高效能網頁圖表呈現，內含 `apply_adjustment` 函數處理價格跳空校正。

## 4. 主要執行流程
1. **每日排程下載 (`main_etl.py`)**：
   - 設定當日日期，程式先檢查資料湖 (默認為 `D:\txf-data`) 是否已存在當日的 `Tick.parquet`。
   - 若無，透過 `ShioajiSource` 連線獲取該商品當日逐筆。
   - 將取得之 Tick 進入 Transform 階段，透過 `resample_to_kbars` 一口氣產生 5s, 1m, 5m, 1h, 1d 的 DataFrame。
   - 將生成之 DataFrame 分別存回日切 (Daily) 或年切 (Yearly, 如日線) 的目錄架構中。
2. **批次回補 (`batch_run.py` / `mxf_batch_run.py`)**：
   - `batch_run.py` 用於補齊 `TXF` 與 `TSE` 的歷史資料。
   - `mxf_batch_run.py` 用於補齊 `MXF` 的回測資料，與日常 ETL 分流。
3. **視覺化查詢 (`view_chart.py`)**：
   - 接受終端機引數 (如 `--tf 1m --date 2025-01-01 --end-date 2025-01-31 --adjust`)。
   - `loader` 查找並合併範圍內的 Parquet 檔案。
   - (若開啟 `--adjust`) 加入外部校正表 (Adjustment Table)，透過 asof join 進行價格平滑處理。
   - 給定 DataFrame，`processor` 生成均線與技術分析。
   - 初始化本地 WebView 彈出並畫出高流暢度的日 / 夜 / 合併走勢圖。

## 5. 技術棧 (Tech Stack)
- **語言**：`Python 3.10+`
- **資料處理關鍵字**：
  - **`Polars`**：取代傳統 Pandas 作為主力 DataFrame 引擎，提供極快的 Tick 到 K 棒聚合速度與內存管理優勢。
  - **`Pandas`**：部分套件相容性轉換或輕量化日期操作時輔助使用。
  - **`Parquet`**：欄位式高效存儲格式。
- **外部連線**：
  - **`shioaji`**：撈取永豐金即時與歷史報價。
- **資料視覺化**：
  - **`lightweight-charts`** (`lightweight-charts-python` wrapper)：TradingView 開源的超高效網頁輕量圖表引擎。
- **環境設定**：`python-dotenv` 讀取隱私 `.env` 以及標準的 `argparse` 用於 CLI 控制。
